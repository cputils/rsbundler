{
  "mustContain": [
    "#[cfg(unix)]",
    "#[cfg(not(unix))]",
    "pub fn unix() {}",
    "pub fn fallback() {}"
  ],
  "mustIncludeFiles": ["unix.rs", "platform.rs"],
  "runtimeChecks": [
    { "rustcArgs": ["--cfg", "unix", "-Aexplicit_builtin_cfgs_in_flags"] }
  ]
}
